{
  "name": "root",
  "private": true,
  "scripts": {
    "precommit": "lerna run --concurrency 1 --stream precommit",
    "docker:up": "sh scripts/docker-mysql-up.sh"
  },
  "devDependencies": {
    "husky": "^0.14.3",
    "lerna": "^3.3.1"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
